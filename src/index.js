import React from 'react';
import ReactDOM from 'react-dom/client'; // <--- Ensure the '/client' is here
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);